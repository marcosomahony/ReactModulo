import React, {Component} from 'react';

const Header = ({logo}) => {
    return (
        <header className="App-header">
            <img src={logo} className="App-logo" alt="logo" />
            <h1 className="App-title">Holaaaaa</h1>
        </header>
    )
}

export default Header;